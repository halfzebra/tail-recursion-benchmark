{
  "name": "tail-recursion-benchmark",
  "private": true,
  "scripts": {
    "test": "jest --watch",
    "bench": "cross-env node benchmarks/index.js"
  },
  "devDependencies": {
    "benchmark": "2.1.4",
    "cross-env": "5.2.1",
    "jest": "24.8.0"
  }
}
